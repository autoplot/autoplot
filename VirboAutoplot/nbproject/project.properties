application.args=
application.title=virboAutoplot
application.vendor=jbf
build.classes.dir=${build.dir}/classes
build.classes.excludes=**/*.java,**/*.form
# This directory is removed when the project is cleaned:
build.dir=build
build.generated.dir=${build.dir}/generated
# Only compile against the classpath explicitly listed here:
build.sysclasspath=ignore
build.test.classes.dir=${build.dir}/test/classes
build.test.results.dir=${build.dir}/test/results
debug.classpath=\
    ${run.classpath}
debug.test.classpath=\
    ${run.test.classpath}
# This directory is removed when the project is cleaned:
dist.dir=dist
dist.jar=${dist.dir}/VirboAutoplot.jar
dist.javadoc.dir=${dist.dir}/javadoc
excludes=
includes=**
jar.compress=false
javac.classpath=\
    ${libs.VirboDataSourcePack.classpath}:\
    ${libs.QDataSet.classpath}:\
    ${libs.APbeanbinding.classpath}:\
    ${libs.APJFontChooser.classpath}:\
    ${libs.VirboDataSource.classpath}:\
    ${libs.CefDataSource.classpath}:\
    ${libs.APAbsoluteLayout.classpath}:\
    ${libs.APBatikSVGGen1.5.classpath}:\
    ${libs.APCdfJava.classpath}:\
    ${libs.APiText.classpath}:\
    ${libs.APJakartaPoi.classpath}:\
    ${libs.APNetCDF.classpath}:\
    ${libs.APSwingLayoutExtensions.classpath}:\
    ${libs.APXerces.classpath}:\
    ${libs.DasCore.classpath}
# Space-separated list of extra javac options
javac.compilerargs=
javac.deprecation=false
javac.source=1.5
javac.target=1.5
javac.test.classpath=\
    ${javac.classpath}:\
    ${build.classes.dir}
javadoc.additionalparam=
javadoc.author=false
javadoc.encoding=
javadoc.noindex=false
javadoc.nonavbar=false
javadoc.notree=false
javadoc.private=false
javadoc.splitindex=true
javadoc.use=true
javadoc.version=false
javadoc.windowtitle=
# Property libs.APAbsoluteLayout.classpath is set here just to make sharing of project simpler.
# The library definition has always preference over this property.
libs.APAbsoluteLayout.classpath=../APLibs/lib/AbsoluteLayout.jar
# Property libs.APBatikSVGGen1.5.classpath is set here just to make sharing of project simpler.
# The library definition has always preference over this property.
libs.APBatikSVGGen1.5.classpath=../APLibs/lib/batik-awt-util.jar;../APLibs/lib/batik-svggen.jar;../APLibs/lib/batik-util.jar
# Property libs.APbeanbinding.classpath is set here just to make sharing of project simpler.
# The library definition has always preference over this property.
libs.APbeanbinding.classpath=../APLibs/lib/beansbinding.jar
# Property libs.APCdfJava.classpath is set here just to make sharing of project simpler.
# The library definition has always preference over this property.
libs.APCdfJava.classpath=../APLibs/lib/cdfjava.jar
# Property libs.APiText.classpath is set here just to make sharing of project simpler.
# The library definition has always preference over this property.
libs.APiText.classpath=../APLibs/lib/itext-1.2.jar
# Property libs.APJakartaPoi.classpath is set here just to make sharing of project simpler.
# The library definition has always preference over this property.
libs.APJakartaPoi.classpath=../APLibs/lib/poi-2.5.1-final-20040804.jar;../APLibs/lib/poi-contrib-2.5.1-final-20040804.jar;../APLibs/lib/poi-scratchpad-2.5.1-final-20040804.jar
# Property libs.APJFontChooser.classpath is set here just to make sharing of project simpler.
# The library definition has always preference over this property.
libs.APJFontChooser.classpath=../APLibs/lib/JFontChooser.jar
# Property libs.APNetCDF.classpath is set here just to make sharing of project simpler.
# The library definition has always preference over this property.
libs.APNetCDF.classpath=../APLibs/lib/netCDF/bufr.jar;../APLibs/lib/netCDF/commons-codec-1.3.jar;../APLibs/lib/netCDF/commons-httpclient-3.0.jar;../APLibs/lib/netCDF/commons-logging-1.0.4.jar;../APLibs/lib/netCDF/gnu-regexp-1.1.4.jar;../APLibs/lib/netCDF/grib-5.0.04.jar;../APLibs/lib/netCDF/jdom.jar;../APLibs/lib/netCDF/jpeg2000.jar;../APLibs/lib/netCDF/lucene.jar;../APLibs/lib/netCDF/netcdfUI-2.2.18.jar;../APLibs/lib/netCDF/nlog4j-1.2.21.jar;../APLibs/lib/netCDF/prefsAll.jar;../APLibs/lib/netCDF/resourcesOptional.jar;../APLibs/lib/netCDF/visadNoDods.jar;../APLibs/lib/netCDF/xercesImpl.jar;../APLibs/lib/netCDF/xml-apis.jar
# Property libs.APSwingLayoutExtensions.classpath is set here just to make sharing of project simpler.
# The library definition has always preference over this property.
libs.APSwingLayoutExtensions.classpath=../APLibs/lib/swing-layout-1.0.jar
# Property libs.APXerces.classpath is set here just to make sharing of project simpler.
# The library definition has always preference over this property.
libs.APXerces.classpath=../APLibs/lib/xerces.jar
# Property libs.CefDataSource.classpath is set here just to make sharing of project simpler.
# The library definition has always preference over this property.
libs.CefDataSource.classpath=../CefDataSource/dist/CefDataSource.jar
# Property libs.DasCore.classpath is set here just to make sharing of project simpler.
# The library definition has always preference over this property.
libs.DasCore.classpath=../../pw/dasCore/dist/dasCore.jar
# Property libs.QDataSet.classpath is set here just to make sharing of project simpler.
# The library definition has always preference over this property.
libs.QDataSet.classpath=../QDataSet/dist/QDataSet.jar
# Property libs.VirboDataSource.classpath is set here just to make sharing of project simpler.
# The library definition has always preference over this property.
libs.VirboDataSource.classpath=../DataSource/dist/DataSource.jar
# Property libs.VirboDataSourcePack.classpath is set here just to make sharing of project simpler.
# The library definition has always preference over this property.
libs.VirboDataSourcePack.classpath=../VirboDataSourcePack/dist/VirboDataSourcePack.jar
main.class=org.virbo.autoplot.AutoPlotMatisse
manifest.file=manifest.mf
meta.inf.dir=${src.dir}/META-INF
platform.active=default_platform
run.classpath=\
    ${javac.classpath}:\
    ${build.classes.dir}
# Space-separated list of JVM arguments used when running the project
# (you may also define separate properties like run-sys-prop.name=value instead of -Dname=value
# or test-sys-prop.name=value to set system properties for unit tests):
run.jvmargs=-Xms10m -Xmx1100m -DcdfLib1=dllcdf -DcdfLib2=cdfNativeLibrary -Dautoplot.default.bookmarks=http://autoplot.org/default.bookmarks.xml -Dautoplot.default.recent=http://autoplot.org/default.recent.xml -Djava.util.logging.config.file=logging.properties
run.test.classpath=\
    ${javac.test.classpath}:\
    ${build.test.classes.dir}
src.src.dir=src
test.test.dir=test
