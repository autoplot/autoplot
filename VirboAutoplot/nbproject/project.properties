application.args=
application.title=virboAutoplot
application.vendor=jbf
build.classes.dir=${build.dir}/classes
build.classes.excludes=**/*.java,**/*.form
# This directory is removed when the project is cleaned:
build.dir=build
build.generated.dir=${build.dir}/generated
# Only compile against the classpath explicitly listed here:
build.sysclasspath=ignore
build.test.classes.dir=${build.dir}/test/classes
build.test.results.dir=${build.dir}/test/results
debug.classpath=\
    ${run.classpath}
debug.test.classpath=\
    ${run.test.classpath}
# This directory is removed when the project is cleaned:
dist.dir=dist
dist.jar=${dist.dir}/VirboAutoplot.jar
dist.javadoc.dir=${dist.dir}/javadoc
excludes=
includes=**
jar.compress=false
javac.classpath=\
    ${libs.JakartaPoi.classpath}:\
    ${libs.DasCore.classpath}:\
    ${libs.BatikSVGGen1.5.classpath}:\
    ${libs.iText1.3.classpath}:\
    ${libs.VirboDataSourcePack.classpath}:\
    ${libs.QDataSet.classpath}:\
    ${libs.XercesJ2.classpath}:\
    ${libs.netCDF.classpath}:\
    ${libs.absolutelayout.classpath}:\
    ${libs.swing-layout.classpath}:\
    ${libs.cdfjava.classpath}:\
    ${libs.beanbinding.classpath}:\
    ${libs.JFontChooser.classpath}:\
    ${libs.VirboDataSource.classpath}:\
    ${libs.CefDataSource.classpath}:\
    ${libs.CdfDataSourcePack.classpath}
# Space-separated list of extra javac options
javac.compilerargs=
javac.deprecation=false
javac.source=1.5
javac.target=1.5
javac.test.classpath=\
    ${javac.classpath}:\
    ${build.classes.dir}
javadoc.additionalparam=
javadoc.author=false
javadoc.encoding=
javadoc.noindex=false
javadoc.nonavbar=false
javadoc.notree=false
javadoc.private=false
javadoc.splitindex=true
javadoc.use=true
javadoc.version=false
javadoc.windowtitle=
# Property libs.BatikSVGGen1.5.classpath is set here just to make sharing of project simpler.
# The library definition has always preference over this property.
libs.BatikSVGGen1.5.classpath=../../../pw/das/lib/batik-awt-util.jar;../../../pw/das/lib/batik-svggen.jar;../../../pw/das/lib/batik-util.jar
# Property libs.beanbinding.classpath is set here just to make sharing of project simpler.
# The library definition has always preference over this property.
libs.beanbinding.classpath=../../../../lib/java/beansbinding-0.5.jar
# Property libs.CdfDataSourcePack.classpath is set here just to make sharing of project simpler.
# The library definition has always preference over this property.
libs.CdfDataSourcePack.classpath=../CdfDataSource/dist/CdfDataSource.jar
# Property libs.cdfjava.classpath is set here just to make sharing of project simpler.
# The library definition has always preference over this property.
libs.cdfjava.classpath=../../../lib/java/cdfjava.jar
# Property libs.CefDataSource.classpath is set here just to make sharing of project simpler.
# The library definition has always preference over this property.
libs.CefDataSource.classpath=../CefDataSource/dist/CefDataSource.jar
# Property libs.DasCore.classpath is set here just to make sharing of project simpler.
# The library definition has always preference over this property.
libs.DasCore.classpath=../../pw/dasCore/dist/dasCore.jar
# Property libs.iText1.3.classpath is set here just to make sharing of project simpler.
# The library definition has always preference over this property.
libs.iText1.3.classpath=../../../pw/das/lib/itext-1.2.jar
# Property libs.JakartaPoi.classpath is set here just to make sharing of project simpler.
# The library definition has always preference over this property.
libs.JakartaPoi.classpath=../../../../lib/java/poi-2.5.1-final-20040804.jar
# Property libs.JFontChooser.classpath is set here just to make sharing of project simpler.
# The library definition has always preference over this property.
libs.JFontChooser.classpath=../../../../lib/java/JFontChooser.jar
# Property libs.netCDF.classpath is set here just to make sharing of project simpler.
# The library definition has always preference over this property.
libs.netCDF.classpath=../../../../lib/java/netcdf-2.2.18.jar
# Property libs.QDataSet.classpath is set here just to make sharing of project simpler.
# The library definition has always preference over this property.
libs.QDataSet.classpath=../QDataSet/dist/QDataSet.jar
# Property libs.VirboDataSource.classpath is set here just to make sharing of project simpler.
# The library definition has always preference over this property.
libs.VirboDataSource.classpath=../DataSource/dist/DataSource.jar
# Property libs.VirboDataSourcePack.classpath is set here just to make sharing of project simpler.
# The library definition has always preference over this property.
libs.VirboDataSourcePack.classpath=../VirboDataSourcePack/dist/VirboDataSourcePack.jar
# Property libs.XercesJ2.classpath is set here just to make sharing of project simpler.
# The library definition has always preference over this property.
libs.XercesJ2.classpath=../../../pw/das/lib/xerces.jar
main.class=org.virbo.autoplot.AutoPlotMatisse
manifest.file=manifest.mf
meta.inf.dir=${src.dir}/META-INF
platform.active=JDK_1.5
run.classpath=\
    ${javac.classpath}:\
    ${build.classes.dir}
# Space-separated list of JVM arguments used when running the project
# (you may also define separate properties like run-sys-prop.name=value instead of -Dname=value
# or test-sys-prop.name=value to set system properties for unit tests):
run.jvmargs=-Xms10m -Xmx1024m -DcdfLib1=dllcdf -DcdfLib2=cdfNativeLibrary -Dautoplot.default.bookmarks=http://autoplot.org/default.bookmarks.xml -Dautoplot.default.recent=http://autoplot.org/default.recent.xml -Djava.util.logging.config.file=logging.properties
run.test.classpath=\
    ${javac.test.classpath}:\
    ${build.test.classes.dir}
src.src.dir=src
test.test.dir=test
